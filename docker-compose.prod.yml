version: '3.8'

services:
  api:
    build:
      context: .
      dockerfile: Dockerfile.Prod
    ports:
      - "5000:5000"
    env_file:
      - .env
    networks:
      - backend

networks:
  backend:
    driver: bridge
